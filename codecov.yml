coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
        minimum: 75%
    patch:
      default:
        target: 80%
        threshold: 2%
        minimum: 70%

  ignore:
    - "**/*.test.ts"
    - "**/*.config.ts"
    - "**/*.d.ts"
    - "coverage/**/*"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
  require_base: false
  require_head: true
  hide_project_coverage: false
  hide_if_no_changes: true
